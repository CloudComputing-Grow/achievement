spring:
  application:
    name: achievement

  datasource:
    url: jdbc:mysql://${DB_HOST:34.50.60.106}:${DB_PORT:3306}/${DB_NAME:achievement_db}?serverTimezone=Asia/Seoul
    username: ${DB_USER:grow_user}
    password: ${DB_PASSWORD:Grow1234!}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: ${SERVER_PORT:3002}